# Euclideanizer pipeline dependencies
# Install with: pip install -r requirements.txt

# Core
numpy>=1.24.0
torch>=2.0.0

# Config
pyyaml>=6.0

# Plotting and progress
matplotlib>=3.7.0
tqdm>=4.65.0

# Analysis (clustering: dendrograms, FPS, linkage)
scipy>=1.10.0
scikit-learn>=1.2.0

# HPO (optional; required for run_hpo.py)
optuna>=3.0.0

# Setup wizard (optional; required for run_setup_wizard.py)
anthropic>=0.21.0

# Tests (optional)
pytest>=7.0.0
